# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which might contain sensitive data, such as
# password, private keys, and other secrets. Alternatively, you could
# comment out this line if you want to check in your .tfvars files.
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and are not to be committed
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc
.terraform.lock.hcl

# Ignore any .log files that may be generated during terraform operations
*.log

test-files

# Ignore .terraform.lock.hcl to allow different platforms to generate their own dependency lock file
#.terraform.lock.hcl

bash.exe.stackdump